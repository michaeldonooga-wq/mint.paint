<Window x:Class="mint.paint.ResizeCanvasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменить размер холста" 
        Width="300" Height="250"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="WidthLabel" Grid.Row="0" Text="Ширина:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
        <TextBox x:Name="WidthTextBox" Grid.Row="1" Margin="0,0,0,15" Padding="5"/>
        
        <TextBlock x:Name="HeightLabel" Grid.Row="2" Text="Высота:" Margin="0,0,0,5" Foreground="{DynamicResource TextBrush}"/>
        <TextBox x:Name="HeightTextBox" Grid.Row="3" Margin="0,0,0,15" Padding="5"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OKButton" Content="OK" Width="80" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
